# GA4 报告治理

本上下文定义 GA4 异常事件统计与多平台展示之间的共同语言，避免报告口径随着输出平台变化。

## 报告模型

**异常报告**：描述一个指定时间区间内，按正式事件规则计算出的异常量、影响用户、结果率、原因和数据质量状态。
_Avoid_: 周报（仅作为默认的时间预设）

**报告区间**：异常报告统计所覆盖的连续日期范围，日期边界按 Property 报告时区解释且首尾日期均包含。
_Avoid_: 查询窗口、展示周期

**比较区间**：与报告区间进行数量或结果率比较的另一段日期范围，长度和统计口径必须明确。
_Avoid_: 上周（除非确实使用上一完整自然周）

**日期预设**：把常用报告区间映射为明确首尾日期的便捷选择，例如上一完整周或最近 7 个完整日。
_Avoid_: 在渲染器中自行推算日期

**已结束日期**：按 Property 报告时区计算，已经完整结束且不会再把当天数据当作最终值的日期。
_Avoid_: 今天数据、实时窗口

## 输出平台

**规范化报告**：与 GA4、飞书或网页格式无关的异常报告数据模型，是所有展示平台的输入。
_Avoid_: 飞书数据、卡片数据

**指标状态**：说明一个统计值是否可计算及原因的结构化状态，例如可用、分母为零、维度未注册或无数据。
_Avoid_: 用空字符串或 0 猜测不可用

**报告契约**：规范化报告的字段、含义、状态和兼容性约定，展示平台据此消费数据。
_Avoid_: 飞书格式

**规则清单**：为每个正式异常事件声明结果类型、可选分母、原因维度和允许值的版本化治理定义。
_Avoid_: 按事件名后缀推导

**规则版本**：规则清单的明确版本标识，用于解释同一事件在不同时间的统计口径。
_Avoid_: 代码提交号

**报告请求**：描述报告区间、比较区间、Property 时区和可选过滤条件的输入意图，不包含任何平台排版信息。
_Avoid_: 飞书请求

**数据源适配器**：把具体数据源转换为计算模块所需归一化事实的适配器。
_Avoid_: 在计算模块中直接访问 GA4

**质量缺口**：不阻止报告生成、但会限制某项指标解释力的数据问题，例如原因维度未注册或分母为零。
_Avoid_: 查询失败

**报告存储**：保存规范化报告以供之后历史查询的可替换能力。
_Avoid_: 飞书消息历史

**渲染器**：将规范化报告转换为某个平台可展示内容的适配器。
_Avoid_: 在数据层拼接平台文本

**投递适配器**：把已渲染的平台内容发送到目标渠道的适配器。
_Avoid_: 渲染器（渲染与发送是两个职责）

**展示平台**：消费规范化报告并提供用户查看能力的渠道，例如飞书卡片或网页。
_Avoid_: 数据源
